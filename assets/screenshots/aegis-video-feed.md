# Aegis video feed

The README's visual walkthrough now uses `video-feed.svg`, a lightweight reconstruction of the project's actual video/detection surface.

The canonical runtime demo remains `frontend/public/drone_feed.mp4` with its matching `frontend/public/detections.json` timeline.
